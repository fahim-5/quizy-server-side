# Copy this file to .env and fill values or set MONGODB_URI directly.
# Example using provided credentials:
# Replace <PASSWORD> with your actual password (URL-encode if it contains special characters or spaces).

MONGO_USER=fahimbafu_db_user
MONGO_PASS=fahimfaysaldb
MONGO_CLUSTER=cluster0.zruszch.mongodb.net
MONGO_DB=onlinequizplatfrom

# Full connection string (recommended):
# MONGODB_URI=mongodb+srv://${MONGO_USER}:${MONGO_PASS}@${MONGO_CLUSTER}/${MONGO_DB}?retryWrites=true&w=majority&appName=Cluster0

# Alternatively set MONGODB_URI directly below (uncomment and paste the final URI):
# MONGODB_URI=

# Notes:
# - If your password contains special characters or spaces, url-encode them (e.g., space -> %20).
# - Do NOT commit your real .env file to source control.
